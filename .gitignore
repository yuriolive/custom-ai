# dependencies
node_modules/
.pnp
.pnp.*

# next.js
.next/
out/
next-env.d.ts

# production
build/
dist/

# misc
.DS_Store
*.pem
*.tsbuildinfo

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm

# env — NEVER commit real values. .env.example is the only tracked env file.
.env
.env.*
!.env.example

# testing / coverage
coverage/

# editors
.vscode/
.idea/

# supabase local state (schema itself is tracked by its owner)
supabase/.branches/
supabase/.temp/

# python (tools/modal)
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/

.vercel
# (vercel link appended `.env*` here; removed — it sat AFTER the
#  !.env.example negation above, and last-match-wins would have
#  re-ignored a file this repo deliberately tracks.)
